[package]
name = "lagguard-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
lagguard-core = { path = "../lagguard-core" }
lagguard-por = { path = "../lagguard-por" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
